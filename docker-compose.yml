# ERakshak — local orchestration. Core = api + dashboard.
# Postgres is included but optional (prototype runs in-memory); enable DATABASE_URL to use it.
services:
  api:
    build: .
    command: uvicorn backend.app.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    # Seed credentials and the JWT secret. Unset, the API generates a random
    # password and logs it once — fine locally, useless behind compose where
    # nobody reads the log. Set them in .env; never commit real values.
    environment:
      ERAKSHAK_JWT_SECRET: ${ERAKSHAK_JWT_SECRET:-dev-only-change-me}
      ERAKSHAK_ANALYST_PASSWORD: ${ERAKSHAK_ANALYST_PASSWORD:-analyst}
      ERAKSHAK_ADMIN_PASSWORD: ${ERAKSHAK_ADMIN_PASSWORD:-admin}
      # Natural-language query. Absent, /v1/query falls back to the offline
      # rule-based engine — the API never hard-depends on network access.
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    volumes:
      - ./datasets:/app/datasets
      - ./data:/app/data
      - ./config:/app/config
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=4).status==200 else 1)"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3

  dashboard:
    build: .
    command: streamlit run backend/app/dashboard/app.py --server.address 0.0.0.0 --server.port 8501 --server.headless true
    ports:
      - "8501:8501"
    volumes:
      - ./datasets:/app/datasets
      - ./data:/app/data
      - ./config:/app/config
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8501/_stcore/health', timeout=4).status==200 else 1)"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  # Optional relational store (scale-triggered). Not required for the prototype.
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: erakshak
  #     POSTGRES_PASSWORD: erakshak
  #     POSTGRES_DB: erakshak
  #   ports: ["5432:5432"]
  #   volumes: ["pgdata:/var/lib/postgresql/data"]

# volumes:
#   pgdata:
